{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "amxx-build",
            "type": "shell",
            "command": "D:/programing/pawn/compiler/amxxpc.exe ${file} -i${workspaceFolder}/include -o${workspaceFolder}/bin/${fileBasenameNoExtension}.amxx",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}